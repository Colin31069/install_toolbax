<ui:FluentWindow x:Class="InstallToolbox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:viewmodels="clr-namespace:InstallToolbox.ViewModels"
        xmlns:models="clr-namespace:InstallToolbox.Models"
        mc:Ignorable="d"
        Title="Windows 軟體安裝管理工具" Height="650" Width="950" 
        WindowStartupLocation="CenterScreen"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        ExtendsContentIntoTitleBar="True">
    <ui:FluentWindow.DataContext>
        <viewmodels:MainViewModel />
    </ui:FluentWindow.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- WPF-UI 標題欄 (TitleBar) -->
        <Grid Grid.Row="0" Grid.ColumnSpan="2">
            <ui:TitleBar Title="Windows 軟體自動部署工具 (分組模式)" />
            <ui:Button HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,140,0" 
                       Appearance="Transparent" Command="{Binding ToggleThemeCommand}" ToolTip="切換明暗佈景">
                <ui:SymbolIcon Symbol="WeatherMoon24" FontSize="18"/>
            </ui:Button>
        </Grid>

        <!-- 左側導航 (Groups) -->
        <Border Grid.Row="1" Grid.Column="0" Background="Transparent" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
            <ListBox ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" 
                     Background="Transparent" BorderThickness="0" Margin="10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5,10">
                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- 右側主要區域 -->
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 上方 Presets 區域 -->
            <Border Grid.Row="0" Margin="10,10,10,0" Padding="10" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="快速預設清單：" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                    <ItemsControl ItemsSource="{Binding Presets}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ui:Button Content="{Binding Name}" 
                                        Command="{Binding DataContext.ApplyPresetCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                        CommandParameter="{Binding}" 
                                        Margin="5,0"
                                        Appearance="Primary" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- 下方 Sections 分頁區 -->
            <TabControl Grid.Row="1" ItemsSource="{Binding SelectedGroup.Sections}" SelectedItem="{Binding SelectedSection, Mode=TwoWay}" Margin="10" Background="Transparent" BorderThickness="0">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" FontSize="14" Padding="10,5"/>
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <Grid>
                            <!-- Section 底下的 App 清單 DataGrid -->
                            <DataGrid ItemsSource="{Binding Apps}" 
                                      AutoGenerateColumns="False" CanUserAddRows="False"
                                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="{DynamicResource ControlStrokeColorDefaultBrush}" 
                                      BorderThickness="1" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                                      Background="Transparent" RowBackground="Transparent" RowHeight="45">
                                <DataGrid.Columns>
                                <!-- 勾選框 -->
                                <DataGridTemplateColumn Width="50">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                
                                <!-- 名稱與描述 -->
                                <DataGridTemplateColumn Header="應用程式" Width="250">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <Image Source="{Binding IconPath, TargetNullValue='', FallbackValue=''}" Width="32" Height="32" Margin="0,0,10,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                                    <TextBlock Text="{Binding Description}" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" TextWrapping="Wrap"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- 狀態 -->
                                <DataGridTextColumn Header="狀態" Binding="{Binding Status}" Width="100" IsReadOnly="True" />
                                
                                <!-- 單項進度 -->
                                <DataGridTemplateColumn Header="進度" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ProgressBar Value="{Binding Progress}" Maximum="100" Height="15" Margin="5"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- 錯誤訊息 -->
                                <DataGridTextColumn Header="訊息" Binding="{Binding ErrorMessage}" Width="*" IsReadOnly="True" Foreground="Red" />
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- 空狀態提示：當該 Section 的 Apps 為 0 時顯示 -->
                        <TextBlock Text="此分類目前沒有可顯示的 App" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="16">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Apps.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>

            <!-- 群組或 Section 為 Null 時的空狀態提示 -->
            <TextBlock Grid.Row="1" 
                       Text="請於左側選擇群組或分類不存在" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                       FontSize="18"
                       FontWeight="Bold">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedSection}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <!-- 底部控制列 -->
        <Border Grid.Row="2" Grid.ColumnSpan="2" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding GlobalStatusMessage}" FontWeight="Bold"/>
                    <ProgressBar Value="{Binding GlobalProgress}" Maximum="100" Height="5" Margin="0,5,20,0"
                                 Width="400" HorizontalAlignment="Left" />
                </StackPanel>

                <ui:Button Grid.Column="2" Content="開始安裝勾選項目" Command="{Binding StartBatchInstallCommand}"
                        Width="150" Height="35" Appearance="Primary"  
                        IsEnabled="{Binding IsNotInstalling}"/>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
